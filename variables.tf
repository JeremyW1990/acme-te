# At the root module level, you may declare any variables that would feed into your module calls.
# Declaration of those variables has been omitted since the values are hard-coded in the module declarations.
# You would add any top-level variables here if needed.